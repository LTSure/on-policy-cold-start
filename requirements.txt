accelerate
bitsandbytes
datasets
deepspeed==0.15.0
einops
isort
jsonlines
loralib
huggingface-hub>=0.26.0,<1.0
optimum
packaging
peft
ray[default]==2.12.0
tensorboard
torch
torchmetrics
tqdm
transformers==4.46.1
transformers_stream_generator
# vllm must be 0.6.3 (install separately after torch/CUDA are set up, e.g. `pip install vllm==0.6.3`)
vllm==0.6.3
wandb
wheel
